#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, k, r;
    cin >> n >> k >> r;

    cout << (n - k) * r << endl;

    return 0;
}